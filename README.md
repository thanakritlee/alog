# ALog

An activity logger that logs activity datetime, info, and duration.

## Description

ALog stands for Activity Logger.

The program can be use to track the time spent on an activity. Each activity session log can also include a comment or info of the session such as what was done during that session.

## Where are my logs stored?

All activity logs are stored in `$HOME/.alog/`
